apply plugin: 'groovy'
// 这个项目是用来自定义插件的，没用
repositories {
    jcenter()
    google()
}

dependencies {
    compile gradleApi()
    compile 'com.android.tools.build:gradle:3.6.3'
    compile 'org.javassist:javassist:3.27.0-GA'
}
